# Test Run Log

Every verification run belongs here: hardware-free (L2 native unit tests) and bench runs alike. Bench procedures live in [bench-test-protocol.md](bench-test-protocol.md).

| Date | Scope | Command | Result | Notes |
|------|-------|---------|--------|-------|
| 2026-07-03 | L2 native - 9 suites | `pio test -e native` | 94/94 PASSED | AD-2 (architecture deepening): christmas-tree countdown made testable via clock injection. `CountDownCtx` moved from startController.cpp into startController.h (next to raceTimingData); `tick()` gained a `now` parameter instead of reading `millis()` internally, so the caller passes `millis()` and tests pass a scripted clock. New `test_countdown` suite (5 tests, header-only - no .cpp/mock-serial): full STAGED->Y3->Y2->Y1->GO at 500 ms in GATEDROP/REACTION, PRO skip-to-Y1 at 400 ms, delay boundary (>=), timer re-anchoring, changed() on transition vs hold, CD_IDLE/CD_GO stable. Both firmware targets compile; SC flash byte-identical (pure restructure). |
| 2026-07-03 | L2 native - 8 suites | `pio test -e native` | 89/89 PASSED | AD-1 (architecture deepening): FC sensor capture logic extracted into one lane-indexed `onBeamBreak(lane, nowUs)`; the two ISRs are now one-line adapters passing `micros()`. Internal per-lane state (`finishTime[2]`, `latched[2]`) replaces the duplicated left/right scalars; public interface and `leftFinished`/`rightFinished` externs unchanged. New `test_sensors` suite (8 tests, real sensors.cpp): not-armed ignore, inside-min-time filter + exact-boundary, valid latch of time & flag, latch-once, lane independence, disarm clears, re-arm resets. Covers the LOGIC half of P1-14 (bench still verifies edge polarity / thresholds). Both firmware targets compile; FC flash byte-identical (pure restructure). |
| 2026-07-03 | L2 native - 7 suites | `pio test -e native` | 81/81 PASSED | AD-7 (architecture deepening, closes the outbox chain): FC adopts the shared Outbox - `PendingTx` struct + `winnerAttempts` counter deleted; one-row policy table MSG_WINNER = RETRY_ONCE. This is the fix for the latent winner-retry no-op found in AD-4: the old re-send landed in the duplicate-rejection window every time and never reached the wire; the engine's RETRY_ONCE uses `txResend()` (rearm-in-place), whose wire-level resend is already pinned by `test_retry_once_resends_identical_bytes`. `winnerMask` now local to handleComplete (engine resends from the captured payload). Main loop acts on verdicts for symmetry with the SC (no FATAL FC entries today). Both firmware targets compile (FC flash grows: engine + txResend linked; SC unchanged). |
| 2026-07-03 | L2 native - 7 suites | `pio test -e native` | 81/81 PASSED | AD-6 (architecture deepening, outbox chain): SC adopts the shared Outbox - hand-written `PendingTx` struct deleted; policy table declares RACE_START/FOUL as FATAL (abort codes per ADR-0004), reaction times / display advance as TOLERATED; `queueHeatMsg()` keeps payload construction SC-owned; main loop acts on the verdict visibly (txError + forceIdle). RACING hold now reads `outbox.anyPending()`. No test changes - regression run confirming zero drift. Both firmware targets compile (SC flash grows: engine + txResend now linked; FC unchanged). |
| 2026-07-02 | L2 native - 7 suites | `pio test -e native` | 81/81 PASSED | AD-5 (architecture deepening, outbox chain): new shared Outbox engine (`firmware/lib/shared/outbox.h`) - per-controller policy table (FATAL/TOLERATED/RETRY_ONCE) turns message outcomes into verdicts; engine reports, main loop acts (ADR-0006). New `test_outbox` suite (9 tests, real serialComm.cpp): ACK clears, FATAL abort verdict with entry code, TOLERATED silent clear, RETRY_ONCE identical-bytes resend / give-up / ACK-of-resend, anyPending, untracked ignored, per-heat retry budget reset. Engine not yet wired into controllers - zero production impact. Both firmware targets compile. |
| 2026-07-02 | L2 native - 6 suites | `pio test -e native` | 72/72 PASSED | AD-4 (architecture deepening, outbox chain): `txResend(id)` added to serialComm - re-send from the tracker's captured payload after TX_TIMEOUT/TX_FAILED. `test_serialcomm` 16 -> 21: identical-bytes resend, same-pass-as-timeout rearm (the window where a plain re-enqueue is silently rejected as a duplicate - the FC's current winner retry hits exactly this), in-flight/never-sent rejection, resend-then-ACK. Both firmware targets compile. |
| 2026-07-02 | L2 native - 6 suites | `pio test -e native` | 67/67 PASSED | AD-11 (architecture deepening, closes the state-machine chain): old transition surface (`selfTransition`/`serviceRx`/`rxTransition`) made private in `stateMachine` and `modeSelect`; the old-vs-new equivalence test retired with it (68 -> 67; its purpose - proving migration parity - is complete). ADR-0006 records the declare-intent interface and Outbox direction. Both firmware targets compile; SC flash byte-identical. |
| 2026-07-02 | L2 native - 6 suites | `pio test -e native` | 68/68 PASSED | AD-10 (architecture deepening): FC handlers migrated to the declare-intent interface; RACING/COMPLETE keep their no-edge-consumption behavior via `service(true)`. Found and closed a latent flag hazard: an unconsumed exit flag from one transition could leak into the next state's `takeExit()` (would have disarmed sensors at RACING entry) - `service()` now scrubs stale exit at the top of each pass, pinned by new test `test_stale_exit_not_inherited_by_next_state`. Both firmware targets compile. |
| 2026-07-01 | L2 native - 6 suites | `pio test -e native` | 67/67 PASSED | AD-9 (architecture deepening): SC handlers migrated to the declare-intent interface (`request`/`service`/`takeEntry`); `modeSelect` gained the same shape. No test changes - regression run confirming zero behavioral drift. Both firmware targets compile; SC flash byte-identical. |
| 2026-07-01 | L2 native - 6 suites | `pio test -e native` | 67/67 PASSED | AD-8 (architecture deepening): `test_state_machine` extended 12 -> 20 tests. Now compiles the real `serialComm.cpp` (same pattern as `test_serialcomm`) so the coordinated-transition protocol runs end-to-end: request/service commit-on-ACK, timeout revert, illegal-request rejection, follower commit, holdRx deferral, stale-`rx.State` P0 regression, forceIdle flags, and an old-vs-new interface equivalence check. Both firmware targets compile clean. |
| 2026-06-12 | L2 native -- 6 suites | `pio test -e native` | 59/59 PASSED | Code-review session. New suites: `test_serialcomm` (16 tests -- real `serialComm.cpp` compiled against the scriptable mock in `firmware/test/shared/Arduino.h`: RX parsing, validation, stale-partial flush, TX retry/timeout/priority/dedup) and `test_display` (7 tests -- digit extraction, rounding, clamp). Expanded: `test_compute` (10 -- new pure TimingInputs interface, clamp + missing-reaction cases), `test_timing` (9 -- N4 zero-raceStart guard, micros() wrap). Both firmware targets also compile clean with `--warnings all`. |
| 2026-05-26 | L2 native -- 4 suites | `pio test -e native` | 30/30 PASSED | Fixed 1 bad test (`test_foul_bits_no_bleed_into_winner` -> `test_foul_left_and_right_no_overlap`): foul/winner masks are separate payloads, not same byte. MinGW required; add `C:\ProgramData\mingw64\mingw64\bin` to PATH first. |
